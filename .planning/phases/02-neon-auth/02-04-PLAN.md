---
phase: 02-neon-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - server/src/app.ts
  - server/src/index.ts
  - server/src/auth/better-auth.ts
  - .github/workflows/ci.yml
  - FORK_DIFF.md
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - QUAL-01

must_haves:
  truths:
    - "CI passes with PAPERCLIP_AUTH_PROVIDER=neon (typecheck, unit tests, build)"
    - "Legacy Better Auth path is explicitly removed from app.ts and index.ts"
    - "FORK_DIFF.md documents that legacy removal is complete"
    - "pnpm -r typecheck, pnpm test:run, pnpm build all pass"
  artifacts:
    - path: "server/src/app.ts"
      provides: "No remaining betterAuth conditional — Neon Auth is the only auth path"
      contains: "neon-auth"
    - path: "server/src/index.ts"
      provides: "No PAPERCLIP_AUTH_PROVIDER conditional for better-auth — removed"
      contains: "NEON_AUTH_BASE_URL"
    - path: ".github/workflows/ci.yml"
      provides: "CI sets PAPERCLIP_AUTH_PROVIDER=neon and NEON_AUTH_BASE_URL for test runs"
      contains: "PAPERCLIP_AUTH_PROVIDER"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "server/src/auth/neon-auth.ts"
      via: "PAPERCLIP_AUTH_PROVIDER=neon env var set in CI"
      pattern: "PAPERCLIP_AUTH_PROVIDER.*neon"
---

<objective>
Remove the legacy Better Auth feature-flag path (D-06 removal criteria: AUTH-* + QUAL-01 green in CI), update CI to run with Neon Auth, and verify all quality gates pass. This is the cutover completion and cleanup plan.

Purpose: QUAL-01 — CI must pass with the Neon Auth path as the only auth provider; D-06 legacy removal.
Output: Cleaned app.ts/index.ts (single auth path), CI workflow updated, final FORK_DIFF.md entry.
</objective>

<execution_context>
@/Users/danielvm/Sites/projectclip-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvm/Sites/projectclip-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-neon-auth/02-CONTEXT.md
@.planning/phases/02-neon-auth/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-neon-auth/02-01-SUMMARY.md
@.planning/phases/02-neon-auth/02-02-SUMMARY.md
@.planning/phases/02-neon-auth/02-03-SUMMARY.md

<interfaces>
<!-- Feature flag state after plans 02-01 through 02-03 -->

After plan 02-01, server/src/app.ts contains:
```typescript
const authProvider = process.env.PAPERCLIP_AUTH_PROVIDER ?? "better-auth";
if (authProvider === "neon") {
  // Neon Auth proxy
} else {
  // Legacy Better Auth
}
```

D-06 removal criteria (02-CONTEXT.md):
"Remove the legacy path in a follow-up task within the same phase or explicit cleanup plan"
"once AUTH-* + QUAL-01 green in CI and locally"

QUAL-01 (REQUIREMENTS.md):
"CI runs pnpm install --frozen-lockfile, pnpm -r typecheck, pnpm test:run, pnpm build,
and pnpm test:e2e (when applicable) before release automation proceeds"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy Better Auth path from app.ts and index.ts</name>
  <files>
    server/src/app.ts
    server/src/index.ts
    server/src/auth/better-auth.ts
    FORK_DIFF.md
  </files>
  <read_first>
    - server/src/app.ts (full file — find all PAPERCLIP_AUTH_PROVIDER conditionals and better-auth imports)
    - server/src/index.ts (full file — find all PAPERCLIP_AUTH_PROVIDER conditionals and better-auth usage)
    - server/src/auth/better-auth.ts (full file — check if any non-auth code depends on it before deciding to delete or stub)
    - FORK_DIFF.md (read to add removal entry)
  </read_first>
  <action>
**server/src/app.ts:**

1. Find the `PAPERCLIP_AUTH_PROVIDER` conditional added in plan 02-01.
2. Remove the conditional entirely. Keep ONLY the Neon Auth path:
   ```typescript
   import { createNeonAuthProxyHandler, resolveNeonAuthSession } from "./auth/neon-auth.js";
   app.all("/api/auth/{*authPath}", createNeonAuthProxyHandler());
   app.use(actorMiddleware(db, { resolveSession: resolveNeonAuthSession }));
   ```
3. Remove any imports of `createBetterAuthHandler`, `resolveBetterAuthSession` from `./auth/better-auth.js` if they are now unused.
4. Do not remove any other auth-related middleware or routes.

**server/src/index.ts:**

1. Find the `PAPERCLIP_AUTH_PROVIDER` conditional added in plan 02-01.
2. Remove the conditional. If the only remaining reference to `better-auth.ts` in `index.ts` is inside that conditional, remove the import too.
3. Keep the startup log: `log.info("Auth provider: neon-auth (Neon Auth managed service)")` (simplified — no env var needed since it's now hardcoded to Neon).

**server/src/auth/better-auth.ts:**

Before removing or modifying, check: `grep -r "better-auth" server/src/ | grep -v "node_modules\|__tests__\|better-auth.ts"` to confirm no remaining imports outside the file itself. If no remaining imports:
- Add a `@deprecated` JSDoc comment at the top of the file marking it as removed in Phase 2
- Do NOT delete the file in this task (leave it as a tombstone with the comment for now — physical deletion would be a follow-on cleanup)
- Alternatively, if the codebase is clean enough, delete the file. Use judgment based on whether other tests still import from it.

**FORK_DIFF.md:**

Append to the Phase 2 section:
```
### Phase 2 cleanup (D-06 removal — plan 02-04)
- Legacy Better Auth feature-flag path removed from server/src/app.ts and server/src/index.ts
- PAPERCLIP_AUTH_PROVIDER env var is no longer needed for the server (Neon Auth is now the only path)
- server/src/auth/better-auth.ts retained as deprecated tombstone (can be deleted in Phase 3 cleanup)
- Removal criteria met: AUTH-01..AUTH-05 tests pass; QUAL-01 CI green with Neon Auth
```
  </action>
  <verify>
    <automated>grep -n "better-auth\|PAPERCLIP_AUTH_PROVIDER" /Users/danielvm/Sites/projectclip-gsd/server/src/app.ts /Users/danielvm/Sites/projectclip-gsd/server/src/index.ts 2>&1 | grep -v "deprecated\|tombstone\|comment"</automated>
  </verify>
  <done>
    - grep "PAPERCLIP_AUTH_PROVIDER" server/src/app.ts → 0 matches (conditional removed)
    - grep "PAPERCLIP_AUTH_PROVIDER" server/src/index.ts → 0 matches
    - grep "createNeonAuthProxyHandler" server/src/app.ts → match (Neon Auth is wired)
    - grep "D-06 removal" FORK_DIFF.md → match
    - pnpm -r typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow for Neon Auth env vars (QUAL-01)</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - .github/workflows/ci.yml (full file — find where env vars are set for test runs, typecheck, and build steps)
    - .env.example (check documented Neon Auth env vars to align CI env var names)
  </read_first>
  <action>
Read `.github/workflows/ci.yml` fully. Find the section(s) where environment variables are injected for CI jobs (typically under `env:` at job or step level, or in a `secrets:` context).

Add the following env vars to the CI test steps that run `pnpm -r typecheck`, `pnpm test:run`, and `pnpm build`. These should be set to non-secret test values (Neon Auth is feature-flagged at runtime but env vars must exist to pass startup validation):

```yaml
env:
  # Neon Auth — test/CI values (not production credentials)
  NEON_AUTH_BASE_URL: "https://test-neonauth.example.com"
  NEON_AUTH_COOKIE_SECRET: "ci-test-secret-32chars-xxxxxxxxxx"
  PAPERCLIP_AUTH_PROVIDER: "neon"
  # Existing auth secret — keep for agent JWT path
  BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET || 'ci-test-secret' }}
```

**Important constraint:** `pnpm test:run` (Vitest unit tests) must pass. The `resolveNeonAuthSession` function throws if `NEON_AUTH_BASE_URL` is unset at call time (not at import time), so unit tests that mock fetch will work fine. The CI env var just needs to be set to any non-empty string to pass the guard.

**Do NOT:**
- Add real Neon Auth credentials to CI — these are test/stub values
- Change the test commands themselves
- Modify any CI secrets configuration (that requires GitHub UI)
- Change CI trigger conditions or branch filters

If the CI workflow already sets env vars via a `.env.ci` file or a `dotenv` step, follow that pattern instead of adding inline `env:` blocks.

Also confirm: does the CI run `pnpm test:e2e`? If yes, add a note that E2E tests with real OAuth are out of scope for Phase 2 (OAuth mocking in Playwright is Phase 9 / pilot territory). Add a `--skip` or `if: false` guard on e2e if needed to prevent CI failure.
  </action>
  <verify>
    <automated>grep -n "NEON_AUTH_BASE_URL\|PAPERCLIP_AUTH_PROVIDER" /Users/danielvm/Sites/projectclip-gsd/.github/workflows/ci.yml 2>/dev/null || echo "ci.yml not found or no match"</automated>
  </verify>
  <done>
    - grep "NEON_AUTH_BASE_URL" .github/workflows/ci.yml → match
    - grep "PAPERCLIP_AUTH_PROVIDER.*neon" .github/workflows/ci.yml → match
    - grep "BETTER_AUTH_SECRET" .github/workflows/ci.yml → match (agent JWT path still needs it)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — all Phase 2 quality gates</name>
  <files></files>
  <action>Human checkpoint — verify all Phase 2 deliverables end-to-end before phase is marked complete.</action>
  <verify>See how-to-verify steps below.</verify>
  <done>All 5 verification steps pass and reviewer types "approved".</done>
  <what-built>
    Plans 02-01 through 02-04 are now complete:
    - Server: Neon Auth proxy handler + session resolver (neon-auth.ts)
    - Schema: FK constraints removed from board_api_keys and cli_auth (AUTH-05)
    - SPA: /login route, Google OAuth primary CTA, @ciandt.com domain check, no sign-up
    - Cleanup: Legacy Better Auth path removed; CI updated with Neon Auth env vars
  </what-built>
  <how-to-verify>
    **1. Quality gates (automated — run these first):**
    ```bash
    cd /Users/danielvm/Sites/projectclip-gsd
    pnpm -r typecheck
    pnpm test:run
    pnpm build
    ```
    All three must exit 0 before proceeding.

    **2. Server startup with Neon Auth flag:**
    ```bash
    NEON_AUTH_BASE_URL=https://placeholder.example.com \
    NEON_AUTH_COOKIE_SECRET=test-secret-32-chars-xxxxxxxxxx \
    pnpm dev:server
    ```
    Expected: Server starts, logs show "Auth provider: neon-auth", no crash.

    **3. SPA routing check:**
    Navigate to http://localhost:3100/auth — should redirect to /login.
    Navigate to http://localhost:3100/login — should show login page with "Sign in to Paperclip" title and "Continue with Google" button.

    **4. Sign-up gate check:**
    Try: `curl -X POST http://localhost:3100/api/auth/sign-up/email -d '{"email":"test@test.com","password":"test"}' -H "Content-Type: application/json"`
    Expected: 403 with `{"error":"Sign-up is disabled. Contact your administrator."}`

    **5. Schema check:**
    ```bash
    grep -r "references.*authUsers" packages/db/src/schema/
    ```
    Expected: 0 matches.
  </how-to-verify>
  <resume-signal>Type "approved" when all 5 checks pass, or describe any failures found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI environment → server tests | CI injects stub NEON_AUTH_BASE_URL; test mocks intercept the fetch — real Neon Auth not called in CI |
| Developer env → production Neon Auth | Production NEON_AUTH_BASE_URL must use HTTPS; dev can use shared Neon dev credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Information Disclosure | CI env vars with Neon Auth stub values | accept | Stub values (test URL, fake secret) are not real credentials; no sensitive data leaked if CI config is exposed |
| T-02-19 | Spoofing | Leftover better-auth.ts tombstone | accept | File is deprecated and no longer imported; no code path reaches it; removal tracked in FORK_DIFF.md for Phase 3 cleanup |
| T-02-20 | Denial of Service | E2E tests with OAuth redirects in CI | mitigate | E2E OAuth flows gated out of CI (requires real Google OAuth app); noted in CI workflow comment; Phase 2 E2E scope limited to non-OAuth flows (ASVS L1 does not require live OAuth in CI) |
</threat_model>

<verification>
Final phase verification for Phase 2 (all plans):

1. `pnpm -r typecheck` — 0 errors
2. `pnpm test:run` — all unit tests pass
3. `pnpm build` — production build succeeds
4. `grep -r "references.*authUsers" packages/db/src/schema/` → 0 matches
5. `grep "createNeonAuthProxyHandler" server/src/app.ts` → match
6. `grep "path=\"login\"" ui/src/App.tsx` → match
7. `grep "Continue with Google" ui/src/pages/Auth.tsx` → match
8. `grep "ciandt.com" ui/src/pages/Auth.tsx` → match
9. `grep "Need an account\|Create account" ui/src/pages/Auth.tsx` → 0 matches
10. `grep "NEON_AUTH_BASE_URL" .github/workflows/ci.yml` → match
</verification>

<success_criteria>
- pnpm -r typecheck, pnpm test:run, pnpm build all pass (QUAL-01)
- Server starts with PAPERCLIP_AUTH_PROVIDER removed (Neon Auth is sole path)
- Legacy Better Auth conditional removed from app.ts and index.ts (D-06)
- CI workflow runs with Neon Auth env vars
- Human checkpoint confirms: /login works, /auth redirects, sign-up returns 403, schema has no FK constraints on authUsers
</success_criteria>

<output>
After completion, create `.planning/phases/02-neon-auth/02-04-SUMMARY.md` following the summary template.
</output>
