---
phase: 01-fork-platform-baseline
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - packages/
  - server/
  - ui/
  - cli/
  - scripts/
  - tests/
  - vitest.config.ts
  - .npmrc
  - FORK_DIFF.md
autonomous: true
requirements:
  - BOOT-01
  - BOOT-07
must_haves:
  truths:
    - "Repository root contains the imported Paperclip monorepo layout (workspaces, apps, packages)."
    - "`pnpm install --frozen-lockfile` completes on a clean checkout after import."
    - "`pnpm db:migrate` applies Drizzle migrations successfully against the local/embedded DB path used by `@paperclipai/db` (no pending failure from migration runner)."
    - "`FORK_DIFF.md` exists at repo root with fork provenance and initial divergence notes (default branch, upcoming release tooling)."
  artifacts:
    - path: package.json
      provides: "Root scripts including db:migrate → pnpm --filter @paperclipai/db migrate"
    - path: FORK_DIFF.md
      provides: "Fork provenance, source path, D-01 as-is import note, master→main intent"
    - path: packages/db/
      provides: "Drizzle package unchanged unless later plans edit it"
  key_links:
    - from: "root package.json scripts.db:migrate"
      to: "packages/db/package.json scripts.migrate"
      via: "pnpm --filter @paperclipai/db migrate"
---

<objective>
Import the Paperclip monorepo from the local clone into the Project Clip repository root **as-is** (D-01), establish `FORK_DIFF.md`, and run a **blocking** database migration verification so schema state is known-good before further work.

Purpose: Satisfy BOOT-01 (runnable fork + install) and begin BOOT-07 documentation.

Output: Full application tree at repo root, `FORK_DIFF.md` stub, verified `pnpm db:migrate`.
</objective>

<execution_context>
Executor: follow tasks in order; use `read_first` paths before mutating.
</execution_context>

<context>
@.planning/phases/01-fork-platform-baseline/01-CONTEXT.md
@.planning/phases/01-fork-platform-baseline/01-RESEARCH.md
@projectclip-prd.md (Epic 0 — fork & boot)

**Import source (D-01):** `/Users/danielvm/Sites/paperclip`

**Verified upstream scripts (RESEARCH):**
- Root: `"db:migrate": "pnpm --filter @paperclipai/db migrate"` (`paperclip/package.json`).
- `@paperclipai/db`: `"migrate": "pnpm run check:migrations && tsx src/migrate.ts"` (`packages/db/package.json`).
- `packageManager`: `pnpm@9.15.4` (D-05 — do not bump in Phase 1).
</context>

<tasks>

<task>
  <name>Import Paperclip tree into repo root (D-01)</name>
  <read_first>
    - /Users/danielvm/Sites/paperclip/package.json
    - /Users/danielvm/Sites/paperclip/pnpm-workspace.yaml
    - .planning/phases/01-fork-platform-baseline/01-CONTEXT.md (D-01)
  </read_first>
  <action>
    1. From Project Clip repo root, ensure planning-only files (e.g. `.planning/`, `projectclip-prd.md`, `.cursor/`) are **not** deleted: use `rsync` with excludes, or copy into a temp dir then merge.

    Suggested command (run from Project Clip repo root; adjust if paths differ):

    ```bash
    rsync -a --delete \
      --exclude='.git' \
      --exclude='.planning' \
      --exclude='projectclip-prd.md' \
      --exclude='.cursor' \
      --exclude='.claude' \
      /Users/danielvm/Sites/paperclip/ ./
    ```

    2. Confirm root `package.json` `packageManager` field is exactly `pnpm@9.15.4` per D-05 / RESEARCH (if upstream differs after refresh, **match upstream Paperclip**, not PROJECT.md prose).

    3. Do **not** redesign architecture, rename packages, or rebrand (out of scope for Phase 1 beyond import).
  </action>
  <acceptance_criteria>
    - `test -f package.json && grep -q '"name": "paperclip"' package.json` OR repo root name matches imported root `name` field from Paperclip.
    - `test -f pnpm-workspace.yaml`
    - `test -f packages/db/package.json && grep -q '@paperclipai/db' packages/db/package.json`
    - `FORK_DIFF.md` is **not** asserted in this task — task 2 creates it; do not require FORK_DIFF here.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f package.json && test -f pnpm-workspace.yaml && test -f packages/db/package.json</automated>
  </verify>
</task>

<task>
  <name>Create FORK_DIFF.md stub (BOOT-07, main vs master)</name>
  <read_first>
    - .planning/phases/01-fork-platform-baseline/01-RESEARCH.md (master vs main, release.yml)
  </read_first>
  <action>
    Create **repo root** `FORK_DIFF.md` with these **exact section headings** (markdown `##`) and content bullets:

    - `## Provenance` — source: local Paperclip at `/Users/danielvm/Sites/paperclip`; git remote to be set to Project Clip origin (not upstream), per team practice.
    - `## Default branch` — upstream uses `master`; Project Clip targets **`main`** for release automation (D-06); follow-up plans will retarget workflows.
    - `## Toolchain pins (D-05)` — document `packageManager: pnpm@9.15.4` and CI Node matrix **mirror Paperclip workflows** (PR/release: Node 24, dispatch e2e: Node 20 per RESEARCH) — cite files `.github/workflows/pr.yml` once imported.
    - `## Release tooling (placeholder)` — one paragraph: upstream uses `scripts/release.sh` (CalVer-style publish); Project Clip will add **semantic-release** per BOOT-06 / D-06; coexistence to be documented in plan `01-04`.

    If `FORK_DIFF.md` already exists from a prior attempt, merge missing sections without deleting prior decisions.
  </action>
  <acceptance_criteria>
    - `grep -q '## Provenance' FORK_DIFF.md`
    - `grep -q 'main' FORK_DIFF.md && grep -q 'master' FORK_DIFF.md`
    - `grep -q 'semantic-release' FORK_DIFF.md || grep -q 'release.sh' FORK_DIFF.md`
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f FORK_DIFF.md && grep -q '## Provenance' FORK_DIFF.md</automated>
  </verify>
</task>

<task>
  <name>[BLOCKING] Install and run Drizzle migrations (packages/db)</name>
  <read_first>
    - packages/db/package.json
    - package.json (scripts.db:migrate)
  </read_first>
  <action>
    1. Install dependencies exactly as CI will: `pnpm install --frozen-lockfile` from repo root.

    2. Run **root** migration script (matches upstream): `pnpm db:migrate`

    This invokes `pnpm --filter @paperclipai/db migrate` → `tsx src/migrate.ts` after migration numbering check.

    3. If migration fails due to missing local DB / env, set whatever **non-secret** env vars Paperclip documents for local embedded Postgres (see `packages/db` README or `migrate.ts` imports) until the command exits **0** — do not commit secrets.

    4. If this task required **any** edit under `packages/db/src/schema/` or `packages/db/src/migrations/`, stop after migrate and ensure the next plan wave records the schema change in FORK_DIFF (plan 01-02 / 01-03).
  </action>
  <acceptance_criteria>
    - `grep -q '"db:migrate"' package.json`
    - `pnpm db:migrate` exits 0 (run after `pnpm install --frozen-lockfile`).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm install --frozen-lockfile && pnpm db:migrate</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Dev machine → pnpm registry | Installing packages pulls executable code; supply-chain risk. |
| migrate.ts → local DB | Migration runner can execute DDL; protect DB URL and credentials. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-1-01 | Spoofing | pnpm install | mitigate | Use `--frozen-lockfile`; do not swap registry without documenting in FORK_DIFF. |
| T-1-02 | Tampering | copied tree vs upstream | mitigate | Record source revision in FORK_DIFF (`git -C /Users/danielvm/Sites/paperclip rev-parse HEAD`). |
| T-1-03 | Repudiation | import provenance | mitigate | FORK_DIFF Provenance section + commit hash string. |
| T-1-04 | Information disclosure | logs during migrate | mitigate | Do not echo `DATABASE_URL` or tokens in CI/local logs; redact in shell. |
| T-1-05 | Denial of service | broken migrations | mitigate | Blocking task fails the plan until `pnpm db:migrate` passes. |
| T-1-06 | Elevation | arbitrary scripts in dependencies | accept | Same as upstream Paperclip baseline; defer hardening to later security phase. |
</threat_model>

<verification>
After all tasks: `pnpm install --frozen-lockfile` succeeds; `pnpm db:migrate` exits 0; `FORK_DIFF.md` present with required sections.
</verification>
