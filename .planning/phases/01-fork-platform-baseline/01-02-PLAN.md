---
phase: 01-fork-platform-baseline
plan: "01-02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/
  - server/
  - ui/
  - cli/
  - tests/
  - vitest.config.ts
  - packages/db/src/schema/
  - packages/db/src/migrations/
autonomous: true
requirements:
  - BOOT-02
  - BOOT-03
  - BOOT-04
  - BOOT-05
must_haves:
  truths:
    - "`pnpm -r typecheck` passes for all workspace packages."
    - "`pnpm test:run` (Vitest) passes for all configured projects."
    - "`pnpm build` completes successfully."
    - "`pnpm test:e2e` passes with `PAPERCLIP_E2E_SKIP_LLM=true` when run locally the same way CI sets it (D-04)."
  artifacts:
    - path: vitest.config.ts
      provides: "Workspace test projects list"
    - path: tests/e2e/playwright.config.ts
      provides: "E2E webServer and health URL"
  key_links:
    - from: "pnpm test:run"
      to: "vitest.config.ts test.projects"
      via: "root package.json scripts.test:run"
    - from: "pnpm test:e2e"
      to: "tests/e2e/playwright.config.ts"
      via: "playwright CLI in root scripts"
---

<objective>
Drive the imported codebase to **green quality gates** by fixing failing unit tests, builds, and e2e per **D-02**, without changing upstream CI layout philosophy (that is plan `01-03`).

Purpose: Satisfy BOOT-02 through BOOT-05 before wiring GitHub Actions.

Output: Local proof that typecheck, Vitest, build, and Playwright e2e succeed.
</objective>

<execution_context>
Run full gates after fixes: `pnpm install --frozen-lockfile && pnpm -r typecheck && pnpm test:run && pnpm build && PAPERCLIP_E2E_SKIP_LLM=true pnpm test:e2e` (e2e env per RESEARCH `pr.yml`).
</execution_context>

<context>
@.planning/phases/01-fork-platform-baseline/01-CONTEXT.md (D-02, D-04)
@.planning/phases/01-fork-platform-baseline/01-RESEARCH.md (commands, e2e env)

**Quality gate order (QUAL-01 / PRD §2.5):** install → typecheck → test:run → build → test:e2e.

**E2e policy (D-04):** Match Paperclip — PR job uses `PAPERCLIP_E2E_SKIP_LLM=true`; Playwright installs Chromium with deps in CI (local: `npx playwright install chromium` as needed).
</context>

<tasks>

<task>
  <name>Fix workspace typecheck failures (D-02, BOOT-02)</name>
  <read_first>
    - package.json (scripts.typecheck, scripts.preflight)
    - packages/db/package.json
  </read_first>
  <action>
    1. Run `pnpm install --frozen-lockfile` then `pnpm -r typecheck` from repo root.

    2. For each failing package, fix TypeScript errors with **minimal** changes (prefer test/fixture fixes over production logic changes unless production is objectively wrong).

    3. Do not upgrade major versions of TypeScript, Node types, or frameworks (D-05).

    4. If a failure is in `packages/db` schema types, note it for the blocking migrate task.
  </action>
  <acceptance_criteria>
    - `pnpm -r typecheck` exits 0.
    - `grep -q typecheck package.json`
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm install --frozen-lockfile && pnpm -r typecheck</automated>
  </verify>
</task>

<task>
  <name>Fix Vitest failures (D-02, BOOT-03)</name>
  <read_first>
    - vitest.config.ts
    - package.json (scripts.test:run)
  </read_first>
  <action>
    1. Run `pnpm test:run` from repo root (includes `preflight:workspace-links` per upstream `package.json`).

    2. Apply TDD where applicable: add or adjust tests when fixing regressions; never delete failing tests without replacing coverage.

    3. Prefer scoped runs while iterating: `pnpm exec vitest run --project <name>` only if documented in task notes for the executor.

    4. Ensure final verification is full `pnpm test:run`.
  </action>
  <acceptance_criteria>
    - `pnpm test:run` exits 0.
    - `grep -q '"test:run"' package.json`
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm test:run</automated>
  </verify>
</task>

<task>
  <name>Fix production build failures (D-02, BOOT-04)</name>
  <read_first>
    - package.json (scripts.build)
  </read_first>
  <action>
    1. Run `pnpm build` from repo root.

    2. Fix build errors in failing packages (`pnpm -r build` chain); keep `prebuild` / `check:migrations` behavior intact for `@paperclipai/db` unless a documented exception is added to FORK_DIFF.
  </action>
  <acceptance_criteria>
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm build</automated>
  </verify>
</task>

<task>
  <name>Fix Playwright e2e failures + [BLOCKING] Drizzle migrate (D-02, D-04, BOOT-05)</name>
  <read_first>
    - tests/e2e/playwright.config.ts
    - .planning/phases/01-fork-platform-baseline/01-RESEARCH.md (webServer command)
    - packages/db/package.json
    - package.json (scripts.db:migrate)
  </read_first>
  <action>
    **Part A — e2e (BOOT-05, D-04)**

    1. Install browsers if missing: `npx playwright install --with-deps chromium` (match CI intent from RESEARCH `pr.yml`).

    2. Ensure default instance config directory exists if tests expect it (CI writes `~/.paperclip/instances/default/config.json` — mirror locally if required).

    3. Run: `PAPERCLIP_E2E_SKIP_LLM=true pnpm test:e2e` from repo root.

    4. Fix flakiness **within** Playwright config limits (timeouts, webServer readiness) — do not disable the e2e job pattern (D-04).

    **Part B — [BLOCKING] migrate (after this plan’s edits)**

    5. Run `pnpm install --frozen-lockfile && pnpm db:migrate` (must exit 0). If any file under `packages/db/src/schema/` or `packages/db/src/migrations/` changed, add `## Database / Drizzle` to `FORK_DIFF.md` with files + reason.
  </action>
  <acceptance_criteria>
    - `PAPERCLIP_E2E_SKIP_LLM=true pnpm test:e2e` exits 0.
    - `grep -q 'test:e2e' package.json`
    - `pnpm db:migrate` exits 0 after this plan's edits.
    - If schema/migrations changed: `grep -q '## Database / Drizzle' FORK_DIFF.md`
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && PAPERCLIP_E2E_SKIP_LLM=true pnpm test:e2e && pnpm db:migrate</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Test code → filesystem / network | E2e may hit local server; ensure no production credentials in fixtures. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-2-01 | Information disclosure | e2e traces / reports | mitigate | Do not commit Playwright `test-results/` with secrets; `.gitignore` per upstream. |
| T-2-02 | Tampering | relaxed tests to “go green” | mitigate | D-02 requires real fixes; forbid `test.skip` without linked issue in FORK_DIFF. |
| T-2-03 | Denial of service | e2e hangs | mitigate | Use upstream webServer timeout settings; fix root cause not unbounded waits. |
| T-2-04 | Elevation | arbitrary `exec` in tests | accept | Inherited upstream; note in FORK_DIFF if new exec paths added. |
</threat_model>

<verification>
Full gate script once: `pnpm install --frozen-lockfile && pnpm -r typecheck && pnpm test:run && pnpm build && PAPERCLIP_E2E_SKIP_LLM=true pnpm test:e2e`
</verification>
